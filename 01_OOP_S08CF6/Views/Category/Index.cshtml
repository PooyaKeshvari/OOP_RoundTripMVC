@model IEnumerable<_01_OOP_S08CF6.Controllers.Dtos.CategoryDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<p>Product Index <a href="https://localhost:44309/Product/Index">Product</a>.</p>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoryTitle)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="ShowDetails" asp-route-id="@item.Id">ShowDetails</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
